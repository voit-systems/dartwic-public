---
updated: 2026-05-02T15:18
created: 2026-05-02
---
<!-- Generated by releases/scripts/generate-plugin-reference.mjs. Do not edit by hand. -->
Main Python helper client for interacting with a DARTWIC engine.

# Declaration

```python
class DartwicClient:
```

# Methods

```python
def __init__(self, tempest_client: Optional[TempestClient] = None) -> None

def connect(self, ip: str = "127.0.0.1", port: int = 7000, password: str = "") -> TempestResponse

def disconnect(self) -> TempestResponse

def operation(self, name: str, payload: Optional[Dict[str, Any]] = None, timeout_ms: int = 5000) -> TempestResponse

def search_channel_dataframes(self, query: str = "", limit: int = 100) -> Dict[str, Any]

def get_channel_keys(self, query: str = "", limit: int = 1000) -> Dict[str, Any]

def query_channel_range(self,
        series: Iterable[str],
        from_timestamp: Optional[int] = None,
        to_timestamp: Optional[int] = None,
        data_frame: str = "",
        bucket_count: Optional[int] = None,
        timeout_ms: int = 10000,) -> Dict[str, Any]
```

# Behavior

- Performs runtime compatibility checks after connect.
- Wraps common RAPID query and search operations with normalized Python return shapes.

# Source

- Public Python client package source.
